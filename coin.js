AFRAME .registerComponent('coins')
init: function(){
    for (var i=1; i<=10; i++)(
        const id=coin$(1)

        const posX=Math.random()*100+50;
        const posY=Math.random()*5+5;
        const posZ=Math.random()*60+60
    )
}